import { Routes } from '@angular/router';
import { PorUrlComponent } from './components/por-url/por-url.component';

export const routes: Routes = [
    {path:'por-url', title: 'Titulo dinámico | Mi proyecto',component: PorUrlComponent}
];
